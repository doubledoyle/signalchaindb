import type {Product} from "@/lib/types";import {shoppingLinks} from "@/lib/affiliate";export function AffiliatePanel({product}:{product:Product}){const links=shoppingLinks(product);return <aside className="shopPanel"><div><span className="kicker">SHOP / COMPARE</span><h2>Find {product.name}</h2><p>Retailer links open in a new tab. Some may be affiliate links that can support SignalChainDB at no extra cost to you.</p></div><div className="shopActions">{links.map(link=><a key={link.label} className="button ghost" href={link.url} target="_blank" rel={link.affiliate?"sponsored nofollow noreferrer":"nofollow noreferrer"}>{link.label} ↗</a>)}</div></aside>}