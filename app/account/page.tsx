import type {Metadata} from "next";import {AccountDashboard} from "@/components/AccountDashboard";export const metadata:Metadata={title:"Account",robots:{index:false,follow:false}};export default function Page(){return <main className="shell page"><div className="pageIntro"><span className="kicker">YOUR SIGNALCHAINDB</span><h1>Rigs that follow you.</h1><p>Keep the active rig in your browser, then save the setups worth keeping to your account.</p></div><AccountDashboard/></main>}