import type {Metadata} from "next";import {PasswordResetForm} from "@/components/PasswordResetForm";export const metadata:Metadata={title:"Reset password",robots:{index:false,follow:false}};export default function Page(){return <main className="shell page narrowPage"><div className="pageIntro"><span className="kicker">ACCOUNT</span><h1>Reset password.</h1><p>Request a reset email or choose a new password after opening the recovery link.</p></div><PasswordResetForm/></main>}