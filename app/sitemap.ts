import type {MetadataRoute} from "next";import {products} from "@/lib/data";export default function sitemap():MetadataRoute.Sitemap{const base=(process.env.NEXT_PUBLIC_SITE_URL||"http://localhost:3000").replace(/\/$/,"");const fixed=["","/gear","/compatibility","/rig-builder","/pricing","/about","/partners","/contact","/affiliate-disclosure","/privacy","/terms"];return [...fixed.map(path=>({url:`${base}${path}`,lastModified:new Date(),changeFrequency:(path===""?"weekly":"monthly") as "weekly"|"monthly",priority:path===""?1:.7})),...products.map(product=>({url:`${base}/gear/${product.slug}`,lastModified:new Date(),changeFrequency:"monthly" as const,priority:.6}))]}