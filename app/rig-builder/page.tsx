import type {Metadata} from "next";import {products,compatibility,powerOutputs} from "@/lib/data";import {RigBuilder} from "@/components/RigBuilder";export const metadata:Metadata={title:"Rig Builder",description:"Build, save, share, and validate a complete music gear rig."};export default function Page(){return <main className="shell page"><div className="pageIntro"><span className="kicker">RIG BUILDER</span><h1>Build the whole signal chain.</h1><p>Add your gear and validate the system as a whole. Your active rig auto-saves in this browser; accounts add cloud saves, and shared links let someone else inspect the same setup.</p></div><div className="panel"><RigBuilder products={products} relations={compatibility} powerOutputs={powerOutputs}/></div></main>}