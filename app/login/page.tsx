import type {Metadata} from "next";import {AuthForm} from "@/components/AuthForm";export const metadata:Metadata={title:"Sign in",robots:{index:false,follow:false}};export default function Page(){return <main className="shell page narrowPage"><div className="pageIntro"><span className="kicker">ACCOUNT</span><h1>Save your rigs.</h1><p>Create an account for cloud rig saves. The gear database and compatibility checker do not require an account.</p></div><AuthForm/></main>}