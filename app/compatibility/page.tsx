import type {Metadata} from "next";import {products,compatibility} from "@/lib/data";import {CompatibilityChecker} from "@/components/CompatibilityChecker";export const metadata:Metadata={title:"Compatibility Checker"};export default function Page(){return <main className="shell page"><div className="pageIntro"><span className="kicker">COMPATIBILITY</span><h1>Will it work?</h1><p>Check stored relationships for power, MIDI, expression control, footswitches, and physical fit.</p></div><div className="panel"><CompatibilityChecker products={products} relations={compatibility}/></div></main>}