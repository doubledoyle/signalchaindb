import type {Metadata} from "next";import {products} from "@/lib/data";import {GearExplorer} from "@/components/GearExplorer";export const metadata:Metadata={title:"Gear Database"};export default function GearPage(){return <main className="shell page"><div className="pageIntro"><span className="kicker">DATABASE</span><h1>Music gear, structured.</h1><p>Search modelers, pedals, power supplies, controllers, pedalboards, and accessories. Verified records are source-backed; catalog-only entries are clearly marked.</p></div><GearExplorer products={products}/></main>}